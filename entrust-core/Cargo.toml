[package]
name = "entrust-core"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[features]
agent = ["dep:entrust-agent"]

[dependencies]
entrust-agent = { path = "../entrust-agent", optional = true }

anyhow = "1.0"
itertools = { workspace = true }
pathdiff = "0.2"
rand = { workspace = true }
walkdir = "2.5"

[dev-dependencies]
tempfile = "3.13"
